__COMMENT="{{ now() }}"
APP_KEY=helloworld

DB_HOST=localhost
DB_DATABASE=librenms
DB_USERNAME=librenms
DB_PASSWORD="{{ database_password }}"

APP_URL="{{ librenms_domain_proto }}://{{ librenms_domain }}"

{% if librenms_domain_proto == "https" %}
SESSION_SECURE_COOKIE=true
{% endif %}